version: 0.2

phases:
  build:
    commands:
      - aws lambda update-function-code --function-name search-photos --region us-east-1 --s3-bucket ooo2139-assignment2-lambda-functions --s3-key search-photos.zip
      - aws lambda update-function-code --function-name index-photos --region us-east-1 --s3-bucket ooo2139-assignment2-lambda-functions --s3-key index-photos.zip